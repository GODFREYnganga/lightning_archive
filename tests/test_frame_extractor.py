import pytest
from lightning_archive.frame_extractor import extract_frames_at_peak_brightness
import os

def test_extract_frames():
    # Mock video path, assume exists
    output_folder = 'data/stills'
    # This would need a test video
    # frames = extract_frames_at_peak_brightness('test.mp4', output_folder)
    # assert len(frames) == 5
    assert True  # Placeholder
